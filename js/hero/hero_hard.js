function placeHeroHard(game) {
  placeHeroCommon(game);
}

function addHeroListenersHard(game) {
  addHeroListenersCommon(game, attackEnemiesHard, checkEnemyAttackHard);
}

function attackEnemiesHard(game) {
  attackEnemiesCommon(game);
}